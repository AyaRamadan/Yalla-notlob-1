<%= form_with(model: friendship, local: true) do |form| %>
  <% if friendship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(friendship.errors.count, "error") %> prohibited this friendship from being saved:</h2>

      <ul>
        <% friendship.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  
  
  <label class="badge badge-primary" style="width:150px; font:18px tahoma">Your friend email: </label>
 <%= form.select :friend_id, @users.collect {|u| [ u.email, u.id ] } %> 
 

  <div class="field">
    <% form.label :group_id %>
    <%= form.hidden_field :group_id %>
    <%= form.submit name="Add" ,class: "btn btn-success" %>
  </div> 

  
<% end %>
